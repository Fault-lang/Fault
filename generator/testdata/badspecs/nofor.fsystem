system nofor;

import "../simple.fspec";

global fl = new simple.fl;

component drain = states{
    initial: sfunc{
        if !fl.active {
            advance(this.done);
        }
    },
    done: sfunc{
        stay();
    },
};

run {
    drain.initial;
}
