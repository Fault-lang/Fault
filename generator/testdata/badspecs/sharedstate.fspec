spec sharedstate;

def s = stock{ v: 10 };

def inc = flow{
    target: new s,
    fn: func{ target.v <- target.v + 1; },
};

def dec = flow{
    target: new s,
    fn: func{ target.v <- target.v - 1; },
};

run init{
    shared = new s;
    a = new inc;
    b = new dec;
    a.target = shared;
    b.target = shared;
} {
    a.fn | b.fn;
    a.fn | b.fn;
    a.fn | b.fn;
    a.fn | b.fn;
    a.fn | b.fn;
}
