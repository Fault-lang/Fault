spec deconflict;

const conflict = true;

def s = stock{
    value: false,
};

def f = flow{
    conflict: new s,
    fn: func{
        conflict.value = true;
    }
};

run init{ t = new f; } {
    t.fn;
}