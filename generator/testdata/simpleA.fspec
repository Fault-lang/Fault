spec simpleA;

const active = true;

def st = stock{
    value: 30,
};

def fl = flow{
    vault: new st,
    fn: func{
        if vault.value > 4 {
           vault.value <- vault.value - 2;
        }
    },
};

run init{l = new fl;} {
    l.fn;
}